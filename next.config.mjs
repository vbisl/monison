/** @type {import('next').NextConfig} */
const nextConfig = {
    productionBrowserSourceMaps: false, // Production ortamında source map oluşturulmasını devre dışı bırak
  };
  
  export default nextConfig;
  